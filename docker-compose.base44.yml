services:
  db:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: pourture
      POSTGRES_PASSWORD: pourture_dev
      POSTGRES_DB: pourture
    volumes:
      - pgdata:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U pourture"]
      interval: 5s
      timeout: 5s
      retries: 10

  setup:
    image: node:22
    working_dir: /app
    volumes:
      - ./pourture-org-v3:/app
    environment:
      - DATABASE_URL=postgresql://pourture:pourture_dev@db:5432/pourture
      - DIRECT_URL=postgresql://pourture:pourture_dev@db:5432/pourture
    depends_on:
      db:
        condition: service_healthy
    command: sh -c "npm install && npx prisma generate && npx prisma db push --accept-data-loss && node prisma/seed.js"

  app:
    image: node:22
    working_dir: /app
    volumes:
      - ./pourture-org-v3:/app
    ports:
      - "3000:3000"
    environment:
      - DATABASE_URL=postgresql://pourture:pourture_dev@db:5432/pourture
      - DIRECT_URL=postgresql://pourture:pourture_dev@db:5432/pourture
      - BASE44_PUBLIC_HOST_SUFFIX=${BASE44_PUBLIC_HOST_SUFFIX}
    env_file:
      - ./.env.base44-defaults
      - /run/base44/app.env
    depends_on:
      setup:
        condition: service_completed_successfully
    command: sh -c "npm run dev -- -H 0.0.0.0"

volumes:
  pgdata:
